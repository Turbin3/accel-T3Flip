/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { T3_FLIP_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const VRF_CALLBACK_DISCRIMINATOR = new Uint8Array([
  248, 224, 55, 227, 56, 10, 108, 36,
]);

export function getVrfCallbackDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VRF_CALLBACK_DISCRIMINATOR
  );
}

export type VrfCallbackInstruction<
  TProgram extends string = typeof T3_FLIP_PROGRAM_ADDRESS,
  TAccountVrfProgramIdentity extends
    | string
    | AccountMeta<string> = '9irBy75QS2BN81FUgXuHcjqceJJRuc9oDkAe8TKVvvAw',
  TAccountGameState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVrfProgramIdentity extends string
        ? ReadonlySignerAccount<TAccountVrfProgramIdentity> &
            AccountSignerMeta<TAccountVrfProgramIdentity>
        : TAccountVrfProgramIdentity,
      TAccountGameState extends string
        ? WritableAccount<TAccountGameState>
        : TAccountGameState,
      ...TRemainingAccounts,
    ]
  >;

export type VrfCallbackInstructionData = {
  discriminator: ReadonlyUint8Array;
  rnd: ReadonlyUint8Array;
};

export type VrfCallbackInstructionDataArgs = { rnd: ReadonlyUint8Array };

export function getVrfCallbackInstructionDataEncoder(): FixedSizeEncoder<VrfCallbackInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['rnd', fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: VRF_CALLBACK_DISCRIMINATOR })
  );
}

export function getVrfCallbackInstructionDataDecoder(): FixedSizeDecoder<VrfCallbackInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['rnd', fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getVrfCallbackInstructionDataCodec(): FixedSizeCodec<
  VrfCallbackInstructionDataArgs,
  VrfCallbackInstructionData
> {
  return combineCodec(
    getVrfCallbackInstructionDataEncoder(),
    getVrfCallbackInstructionDataDecoder()
  );
}

export type VrfCallbackInput<
  TAccountVrfProgramIdentity extends string = string,
  TAccountGameState extends string = string,
> = {
  vrfProgramIdentity?: TransactionSigner<TAccountVrfProgramIdentity>;
  gameState: Address<TAccountGameState>;
  rnd: VrfCallbackInstructionDataArgs['rnd'];
};

export function getVrfCallbackInstruction<
  TAccountVrfProgramIdentity extends string,
  TAccountGameState extends string,
  TProgramAddress extends Address = typeof T3_FLIP_PROGRAM_ADDRESS,
>(
  input: VrfCallbackInput<TAccountVrfProgramIdentity, TAccountGameState>,
  config?: { programAddress?: TProgramAddress }
): VrfCallbackInstruction<
  TProgramAddress,
  TAccountVrfProgramIdentity,
  TAccountGameState
> {
  // Program address.
  const programAddress = config?.programAddress ?? T3_FLIP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vrfProgramIdentity: {
      value: input.vrfProgramIdentity ?? null,
      isWritable: false,
    },
    gameState: { value: input.gameState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vrfProgramIdentity.value) {
    accounts.vrfProgramIdentity.value =
      '9irBy75QS2BN81FUgXuHcjqceJJRuc9oDkAe8TKVvvAw' as Address<'9irBy75QS2BN81FUgXuHcjqceJJRuc9oDkAe8TKVvvAw'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vrfProgramIdentity),
      getAccountMeta(accounts.gameState),
    ],
    data: getVrfCallbackInstructionDataEncoder().encode(
      args as VrfCallbackInstructionDataArgs
    ),
    programAddress,
  } as VrfCallbackInstruction<
    TProgramAddress,
    TAccountVrfProgramIdentity,
    TAccountGameState
  >);
}

export type ParsedVrfCallbackInstruction<
  TProgram extends string = typeof T3_FLIP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vrfProgramIdentity: TAccountMetas[0];
    gameState: TAccountMetas[1];
  };
  data: VrfCallbackInstructionData;
};

export function parseVrfCallbackInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedVrfCallbackInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vrfProgramIdentity: getNextAccount(),
      gameState: getNextAccount(),
    },
    data: getVrfCallbackInstructionDataDecoder().decode(instruction.data),
  };
}
