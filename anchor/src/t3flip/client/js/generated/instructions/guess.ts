/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableSignerAccount,
} from 'gill';
import { T3_FLIP_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const GUESS_DISCRIMINATOR = new Uint8Array([
  150, 196, 62, 128, 129, 236, 114, 40,
]);

export function getGuessDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GUESS_DISCRIMINATOR);
}

export type GuessInstruction<
  TProgram extends string = typeof T3_FLIP_PROGRAM_ADDRESS,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountGameState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountGameState extends string
        ? ReadonlyAccount<TAccountGameState>
        : TAccountGameState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type GuessInstructionData = {
  discriminator: ReadonlyUint8Array;
  nftId: number;
};

export type GuessInstructionDataArgs = { nftId: number };

export function getGuessInstructionDataEncoder(): FixedSizeEncoder<GuessInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['nftId', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: GUESS_DISCRIMINATOR })
  );
}

export function getGuessInstructionDataDecoder(): FixedSizeDecoder<GuessInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['nftId', getU8Decoder()],
  ]);
}

export function getGuessInstructionDataCodec(): FixedSizeCodec<
  GuessInstructionDataArgs,
  GuessInstructionData
> {
  return combineCodec(
    getGuessInstructionDataEncoder(),
    getGuessInstructionDataDecoder()
  );
}

export type GuessInput<
  TAccountPlayer extends string = string,
  TAccountGameState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  gameState: Address<TAccountGameState>;
  systemProgram?: Address<TAccountSystemProgram>;
  nftId: GuessInstructionDataArgs['nftId'];
};

export function getGuessInstruction<
  TAccountPlayer extends string,
  TAccountGameState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof T3_FLIP_PROGRAM_ADDRESS,
>(
  input: GuessInput<TAccountPlayer, TAccountGameState, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress }
): GuessInstruction<
  TProgramAddress,
  TAccountPlayer,
  TAccountGameState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? T3_FLIP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    gameState: { value: input.gameState ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.gameState),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getGuessInstructionDataEncoder().encode(
      args as GuessInstructionDataArgs
    ),
    programAddress,
  } as GuessInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountGameState,
    TAccountSystemProgram
  >);
}

export type ParsedGuessInstruction<
  TProgram extends string = typeof T3_FLIP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    player: TAccountMetas[0];
    gameState: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: GuessInstructionData;
};

export function parseGuessInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedGuessInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      player: getNextAccount(),
      gameState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getGuessInstructionDataDecoder().decode(instruction.data),
  };
}
