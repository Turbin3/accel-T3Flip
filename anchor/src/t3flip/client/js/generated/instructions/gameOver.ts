/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { T3_FLIP_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const GAME_OVER_DISCRIMINATOR = new Uint8Array([
  91, 212, 164, 201, 191, 57, 99, 179,
]);

export function getGameOverDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GAME_OVER_DISCRIMINATOR);
}

export type GameOverInstruction<
  TProgram extends string = typeof T3_FLIP_PROGRAM_ADDRESS,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountGameState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> &
            AccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountGameState extends string
        ? WritableAccount<TAccountGameState>
        : TAccountGameState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type GameOverInstructionData = { discriminator: ReadonlyUint8Array };

export type GameOverInstructionDataArgs = {};

export function getGameOverInstructionDataEncoder(): FixedSizeEncoder<GameOverInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GAME_OVER_DISCRIMINATOR })
  );
}

export function getGameOverInstructionDataDecoder(): FixedSizeDecoder<GameOverInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGameOverInstructionDataCodec(): FixedSizeCodec<
  GameOverInstructionDataArgs,
  GameOverInstructionData
> {
  return combineCodec(
    getGameOverInstructionDataEncoder(),
    getGameOverInstructionDataDecoder()
  );
}

export type GameOverInput<
  TAccountPlayer extends string = string,
  TAccountGameState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  gameState: Address<TAccountGameState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getGameOverInstruction<
  TAccountPlayer extends string,
  TAccountGameState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof T3_FLIP_PROGRAM_ADDRESS,
>(
  input: GameOverInput<
    TAccountPlayer,
    TAccountGameState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): GameOverInstruction<
  TProgramAddress,
  TAccountPlayer,
  TAccountGameState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? T3_FLIP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    gameState: { value: input.gameState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.gameState),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getGameOverInstructionDataEncoder().encode({}),
    programAddress,
  } as GameOverInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountGameState,
    TAccountSystemProgram
  >);
}

export type ParsedGameOverInstruction<
  TProgram extends string = typeof T3_FLIP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    player: TAccountMetas[0];
    gameState: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: GameOverInstructionData;
};

export function parseGameOverInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedGameOverInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      player: getNextAccount(),
      gameState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getGameOverInstructionDataDecoder().decode(instruction.data),
  };
}
